# Orbia SaaS - Environment Configuration

# ============================================
# DATABASE
# ============================================
DATABASE_URL=postgresql://postgres:123@localhost:5432/orbia

# ============================================
# SERVER
# ============================================
PORT=5000
NODE_ENV=development
SESSION_SECRET=your-secret-key-change-in-production

# ============================================
# AI SERVICE (STT)
# ============================================
# Backend makes requests to this URL
AI_SERVICE_URL=http://127.0.0.1:8001

# STT Security & Cost Controls (Backend)
STT_RATE_LIMIT_PER_MIN=10
STT_CONCURRENCY_PER_TENANT=1
STT_MAX_BASE64_BYTES=2000000
STT_TIMEOUT_MS=30000
STT_RETRY_ON_FAILURE=true
STT_DEBUG=true
# AI Worker Configuration (AI Service)
# Railway will set PORT automatically, this is for local dev
AI_SERVICE_PORT=8001
AI_WORKER_TIMEOUT_SECONDS=25
WHISPER_MODEL=base

# CORS: Backend URL for ai-service (use Railway private domain in production)
# Leave empty for development (allows all origins)
BACKEND_URL=

# CORS: Allowed frontend origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:5000

# ============================================
# SEEDING (development only)
# ============================================
# SEED=false
